{
  "_comment": "Hand-authored C table for the offline physics-faithful capture model (src-tauri/src/sim_device.rs::e2e_capture). Per-slot base + per-scene C = the sound's max-reachable LUFS at presetLevel=1.0 with outputLevel at its stored value; the model shifts by 20*log10(presetLevel) and a scene-relative outputLevel term. Stored knob values are NOT here — they are derived from scenario-presets.json (never duplicated). C values are chosen so the shipped default targets (-26/-24/-22) solve cleanly on these presets. Keys are 0-based LIST INDICES (device userSlot = index+1).",
  "slots": {
    "400": {
      "base": -18.0,
      "scenes": [-19.0, -17.0],
      "routedNode": "ACD_DeluxeReverb65BlondeVibratoNoFxCabIR"
    },
    "401": {
      "_comment": "Ceiling -23: solvable at Crunch/Rhythm (-24/-26, quieter) but CLAMPS at Lead (-22, louder than the ceiling) — the re-level-clamped loop (level-defaults.spec.ts): level at Lead → clamp, re-level at Crunch → resolves to done.",
      "base": -23.0
    },
    "402": { "base": -16.0 },
    "403": {
      "_comment": "E2E Realistic (gtrParallel1). Base C=-28 → CLAMPS at any shipped default target (-26/-24/-22 are all louder than -28), the first-session mass-clamp reality on the presetLevel path (fully faithful). 4 scenes solvable via amp outputLevel (C above the targets). FS1 (BRANCH B) is off-branch: offbranchSwitchNode silences it when engaged.",
      "base": -28.0,
      "scenes": [-22.0, -16.0, -18.0, -17.0],
      "offbranchSwitchNode": "ACD_TubeScreamer"
    }
  },
  "default": -18.0
}
