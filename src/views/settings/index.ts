// src/views/settings/index.ts — barrel for the Settings tab (split from the old
// monolithic src/views/SettingsView.tsx).
export { SettingsView, default } from "./SettingsView";
export type { CategoryId, SettingsViewProps } from "./SettingsView";
