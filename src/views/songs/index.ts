// src/views/songs/index.ts — barrel for the Songs tab (split from the old monolithic
// src/views/SongsView.tsx). Sub-components stay PRIVATE (not re-exported here).
export { SongsView, default } from "./SongsView";
